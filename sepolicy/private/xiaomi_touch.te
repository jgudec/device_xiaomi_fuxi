type xiaomi_touch, domain, coredomain;
type xiaomi_touch_exec, file_type, exec_type, system_file_type;
type vendor_touchfeature_device, dev_type;

allow init vendor_touchfeature_device:chr_file { ioctl };

init_daemon_domain(xiaomi_touch)

allow xiaomi_touch vendor_touchfeature_device:chr_file rw_file_perms;
