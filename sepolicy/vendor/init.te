allow init init:capability2 { wake_alarm };

allow init init:netlink_generic_socket { read };
allow init init:netlink_kobject_uevent_socket { read };

allow init init:qipcrtr_socket { write };
allow init init:qipcrtr_socket { read };

allow init vendor_sysfs_graphics:file { write };
allow init vendor_sysfs_graphics:file { open };

allow init same_process_hal_file:file { execute };

allow init hal_graphics_composer_default:binder { call };
allow init hal_graphics_composer_default:binder { transfer };
allow init system_server:binder { transfer };

allow init vendor_file:file { execute };
allow init init:qipcrtr_socket { create };
allow init init:qipcrtr_socket { getattr };
allow init init:qipcrtr_socket { setopt };

allow init hwservicemanager:binder { call };
allow init system_server:binder { call };

allow init graphics_device:chr_file { ioctl };
allow init untrusted_app:binder { transfer };
allow init vendor_persist_sensors_file:file { watch watch_reads };

allow init platform_app:binder { transfer };
